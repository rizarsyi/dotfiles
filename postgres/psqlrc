\pset null 'NULL'

\set PROMPT1 '%n@%M:%>%x %/ ~> '
\set PROMPT2 ''
\set AUTOCOMMIT off
\set eav 'EXPLAIN ANALYZE VERBOSE'
\set HISTSIZE 20
\set HISTFILE ~/.psql_history- :DBNAME

\timing on
\pset pager always
